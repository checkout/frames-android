package com.checkout.frames.model

public data class Padding(
    /** Top padding in dp. */
    val top: Int = 0,
    /** Bottom padding in dp. */
    val bottom: Int = 0,
    /** Start padding in dp. */
    val start: Int = 0,
    /** End padding in dp. */
    val end: Int = 0
)
